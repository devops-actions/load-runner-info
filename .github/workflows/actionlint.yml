name: Lint GitHub Actions workflows
on: [push, pull_request]

permissions:
  contents: read
  pull-requests: write

jobs:
  actionlint:
    uses: devops-actions/.github/.github/workflows/actionlint.yml@3bb5c8505eb368f90cf87956f0cc4d84cb8554d1 # main
    permissions:
      contents: read
      pull-requests: write
