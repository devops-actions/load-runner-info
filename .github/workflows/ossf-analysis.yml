name: Scorecards supply-chain security scan
on:
  # Only the default branch is supported.
  branch_protection_rule:
  schedule:
    # Weekly on Saturdays.
    - cron: '34 1 * * 6'
  push:
    branches: main
  workflow_dispatch:
    
# Declare default GITHUB_TOKEN permissions as read only.
permissions:
  contents: read

jobs:
  analysis:
    uses: devops-actions/.github/.github/workflows/rw-ossf-scorecard.yml@3bb5c8505eb368f90cf87956f0cc4d84cb8554d1 # main
    permissions:
      # Needed to upload the results to code-scanning dashboard.
      security-events: write
      id-token: write
      actions: read
      contents: read
